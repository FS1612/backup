#include <stdio.h>
#include <stdlib.h>
int main (void)
{
	int a, b;
	float c;
	printf("immetti a\n");
	scanf("%d", &a );
	c = a % 2 ;
	printf("n:%f\n\n",c);
	if ( c == 0 )
{
		printf("pari\n");
}
	else ;
	printf("numero brutto\n\a");


	exit(0);
}